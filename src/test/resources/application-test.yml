spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      enable-auto-commit: false
      group-id: bootcamp
      max-poll-records: 1
    template:
      default-topic: example-kafka-topic
